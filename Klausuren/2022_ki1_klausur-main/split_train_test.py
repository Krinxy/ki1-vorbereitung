def split_data(data, ratio):
    assert(ratio <= 1.0)
    assert(ratio >= 0.0)
    # NOTE: ratio > 0.5 means that the first list is bigger

    # TODO: Implement splitting the list "data" here
    # TODO: return both parts as lists
